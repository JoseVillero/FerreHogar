package com.adsosena.egloapps.entities;

/**Enum Rol:
 * Esta enum representa una entidad (tabla) en la base de datos.
 * Los atributos son representados como columnas en la base de datos
 * @author Jose David */
public enum Rol {

    ADMIN, USER
}
